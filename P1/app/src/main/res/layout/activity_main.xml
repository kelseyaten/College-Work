<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity"
    android:background="#39070305">

    <TextView android:text="How would you like to search today?" android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/textView"
        android:textSize="@dimen/abc_action_bar_stacked_max_height"
        android:textAlignment="center"
        android:gravity="center_horizontal"
        android:autoText="false"
        android:background="#02ffffff" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Artist Name"
        android:id="@+id/button"
        android:layout_alignParentBottom="true"
        android:textSize="@dimen/abc_text_size_large_material"
        android:onClick="buttonOnClickArtist"
        android:background="#20070305" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Song Name"
        android:id="@+id/Song"
        android:textSize="@dimen/abc_text_size_large_material"
        android:onClick="buttonOnClickSong"
        android:layout_alignTop="@+id/button"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:background="#20070305" />

</RelativeLayout>
